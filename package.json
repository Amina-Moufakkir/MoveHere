{
  "name": "movehere",
  "version": "0.0.0",
  "private": true,
  "description": "Park-first, venue-aware workout generation. Phase 0 domain contracts.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:contracts": "node scripts/check-contracts.mjs",
    "test:runtime": "node --experimental-strip-types --test tests/runtime/*.test.ts",
    "check:feasibility": "node scripts/check-feasibility.mjs",
    "verify": "npm run typecheck && npm run test:contracts && npm run test:runtime && npm run check:feasibility"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "type": "module"
}
